version: "1.0"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "3306:3306"
    environment:
      DB_PASSWORD: ""
      DB_USERNAME: "root"
      DB_HOST: "host.docker.internal"
      SCOPE: "develop-write"